# Paste the code above, save (Ctrl+O, Enter, Ctrl+X){
rm -rf node_modules package-lock.json "name": "fitech-token", "version": "1.0.0", "description": "Fitech 
    Staking Project", "devDependencies": {
npm install        "@nomicfoundation/hardhat-chai-matchers": "^2.0.8",
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "@nomicfoundation/hardhat-ignition": "^0.15.0",
        "hardhat": "^2.23.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.2",
        "dotenv": "^16.4.5",
        "ethers": "^6.14.3"
    },
    "scripts": {
        "test": "npx hardhat test",
        "compile": "npx hardhat compile",
        "deploy": "npx hardhat ignition deploy ignition/modules/FitechDeployment.js"
    }
}
npx hardhat clean
rm -rf cache artifacts

